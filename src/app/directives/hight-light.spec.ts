import { HightLight } from './hight-light';

describe('HightLight', () => {
  it('should create an instance', () => {
    const directive = new HightLight();
    expect(directive).toBeTruthy();
  });
});
